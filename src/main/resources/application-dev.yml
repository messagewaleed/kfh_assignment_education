spring:
    jpa:
        hibernate:
          ddl-auto: update
    datasource:
        url: jdbc:mysql://ourInstanceOnDev/kfh_assign
        username: ${SPRING_DATASOURCE_USERNAME}
        password: ${SPRING_DATASOURCE_PASSWORD}